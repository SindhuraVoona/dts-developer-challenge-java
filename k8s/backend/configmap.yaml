apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: task-manager
data:
  # Points to the database-service Service name within the cluster
  SERVICES_DATABASE_SERVICE_BASE_URL: http://database-service:8081
