apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: task-manager
spec:
  selector:
    app: backend-service
  ports:
    - port: 8080
      targetPort: 8080
  # ClusterIP — exposed externally via Ingress only
  type: ClusterIP
